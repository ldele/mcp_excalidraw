# Normalize line endings to LF in the index on every platform. Guards the CRLF/mount-truncation
# hazard (silent corruption when a repo is shared across Windows and Linux mounts) — see ADR-006.
* text=auto eol=lf
